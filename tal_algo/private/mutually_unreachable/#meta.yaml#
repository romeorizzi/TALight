public_folder: public
services:
    solve:
        evaluator: [python, manager.py]
        args:
            size:
                regex: ^(esempi_testo|small|medium|big)$
                default: big
            small:
                regex: ^(numero|all)$
                default: all
        files:
            - source