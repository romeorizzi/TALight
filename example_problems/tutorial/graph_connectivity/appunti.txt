networkx

# Your graph is NOT connected. Let me convince you about this fact. Consider the following partition of the nodes:
# set 1:
0 3 6 7 8
# set 2:
1 2 4 5 9 10

# Your graph is connected. Indeed, there is a way for every node to get to node 0. Here is a valid routing table:
# if_you_are_in then_go
1 3
2 3
3 0
...

def print_path_to_node(v):
  if dad[v] == v:
    print(v)
  print_path_to_node(dad[v]):
    print[v]

def print_path_to_node(v):
  if dad[v] != v:
    print_path_to_node(dad[v]):
  print(v)


check_certificate_of_connectivity:
mi incazzo se la riga non è una coppia di numeri e se i numeri non sono dentro il range [0,n) 

f"no, la tua tabella a {len(routing_table)} righe invece che n={n}"
f"no, la tua tabella fornisce due indicazione relative allo stesso nodo"
f"no, seguendo la tua tabella mi sono ristrovato nel seguente ciclo: "  5 7 2 6 5

check_certificate_of_nonconnectivity:

aggiungere servizi:
eval_your_check_certificate_of_connectivity:
eval_your_check_certificate_of_nonconnectivity:

