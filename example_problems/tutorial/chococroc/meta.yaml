%YAML 1.2
---
public_folder: public
services:
  check_is_winning:
    description: use this service to check out your belief about a certain configuration to be a winning one.
    evaluator: [python, services/check_game_value_server.py]
    args:
      m:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of rows of the chocolate bar.
      n:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of columns of the chocolate bar.
      value:
        regex: ^-2$
        default: -2
        explain: Your bet is that the (m,n) configuration is a winning one.
      silent:
        regex: ^(0|1)$
        default: 0
        explain: If this flag is set to 1 nothing is printed unless your conjecture is a false one.
      lang:
        regex: ^(hardcoded|hardcoded_ext|en|it)$
        default: it
  check_is_lost:
    description: use this service to check out your belief about a certain configuration to be a lost one.
    evaluator: [python, services/check_game_value_server.py]
    args:
      m:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of rows of the chocolate bar.
      n:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of columns of the chocolate bar.
      value:
        regex: ^-1$
        default: -1
        explain: Your bet is that the (m,n) configuration is a lost one.
      silent:
        regex: ^(0|1)$
        default: 0
        explain: If this flag is set to 1 nothing is printed unless your conjecture is a false one.
      lang:
        regex: ^(hardcoded|hardcoded_ext|en|it)$
        default: it
  check_grundy_value:
    description: use this service to check out your conjectured grundy value for a certain configuration of the chococroc game.
    evaluator: [python, services/check_game_value_server.py]
    args:
      m:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of rows of the chocolate bar.
      n:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of columns of the chocolate bar.
      value:
        regex: ^(0|[1-9][0-9]{0,50})$
        default: 0
        explain: The grundy value that you are guessing for a chocolate bar of m rows and n columns.
      silent:
        regex: ^(0|1)$
        default: 0
        explain: If this flag is set to 1 then nothing is printed unless your conjecture is a false one.
      lang:
        regex: ^(hardcoded|hardcoded_ext|en|it)$
        default: it
  tell_me_about_config:
    description: use with caution this spoilering service in order to get more or less detailed information about a certain configuration of the chococroc game.
    evaluator: [python, services/tell_me_about_config_server.py]
    args:
      m:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of rows of the chocolate bar.
      n:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of columns of the chocolate bar.
      info_requested:
        regex: ^(won_or_lost|grundy_val|gimme_a_winning_move|gimme_all_winning_moves)$
        default: won_or_lost
        explain: "the options are as follows:"
        explain1: -won_or_lost= just tell me whether the given configuration m x n is a winning or lost one.
        explain2: -grundy_val= please, tell me the grundy value of the given configuration m x n.
        explain3: -gimme_a_winning_move= if the given configuration m x n is winning, please, suggest me a winning move.
        explain4: -gimme_all_winning_moves= give me the whole set of winning moves for the given m x n configuration.
      lang:
        regex: ^(hardcoded|hardcoded_ext|en|it)$
        default: it
  play:
    description: 'use this service if you want to play a match of the chococroc game against our player. You can choose the starting configuration and whether to be first or second to move. You can also ask for supporting information. Finally, as with other interactive TALight services, you can plug in a bot of your to play in your behalf.\n   Example of direct use:\n    rtal connect chococroc play -aplayer=1 -am=10 -an=10 -awatch_value=no_watch -- bots/my_player.py -player=0\n   Example test your bot:\n    rtal connect chococroc play -aplayer=1 -am=10 -an=10 -awatch_value=no_watch -- bots/my_player.py -player=1'
    example1: 'Example of direct use: rtal connect chococroc play -aplayer=1 -am=10 -an=10 -awatch_value=no_watch'
    example2: 'Example test your bot: rtal connect chococroc play -aplayer=1 -am=10 -an=10 -awatch_value=no_watch -- bots/my_player.py -player=1'
    #example1: [Example of direct use, rtal connect chococroc play -aplayer=1 -am=10 -an=10 -awatch_value=no_watch]
    #example2: [Example test your bot, rtal connect chococroc play -aplayer=1 -am=10 -an=10 -awatch_value=no_watch -- bots/my_player.py -player=1]
    evaluator: [python, services/play_server.py]
    args:
      m:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of rows of the chocolate bar.
      n:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of columns of the chocolate bar.
      player:
        regex: ^(0|1)$
        default: 0
        explain: If you to start as first player use 0, if you want to start as second player use 1.
      watch_value:
        regex: ^(no_watch|watch_winner|num_winning_moves|list_winning_moves|watch_grundy_val)$
        default: no_watch
        explain: With this flag you set which informations should be displayed about any configuration that you will face during your match.
      lang:
        regex: ^(hardcoded|hardcoded_ext|en|it)$
        default: it
  play_val_measuring_game:
    description: 'use this service if you want to play a match of the MeasuringGame(chococroc) against our player. You can choose the starting configuration and whether to be first or second to move. The MeasuringGame(chococroc) game is defined as the game sum of a chococroc game and a Nim game with one single pile. As such, it can be used to assess the Grundy value of a chococroc game position. You can choose whether to be first or second to move in the play. You can also ask for supporting information. Finally, as with other interactive TALight services, you can plug in a bot of your to play in your behalf.\n   Example of direct use:\n    rtal connect chococroc play_val_measuring_game -aplayer=1 -am=10 -an=10 -anim=0 -awatch_value=no_watch -- bots/my_player.py -player=0\n   Example test your bot:\n    rtal connect chococroc play_val_measuring_game -aplayer=1 -am=10 -an=10 -anim=0 -awatch_value=no_watch -- bots/my_player.py -player=1' 
    evaluator: [python, services/play_val_measuring_game_server.py]
    args:
      m:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of rows of the chocolate bar.
      n:
        regex: ^([1-9][0-9]{0,50})$
        default: 1
        explain: Number of columns of the chocolate bar.
      nim:
        regex: ^([0-9][0-9]{0,50})$
        default: 0
        explain: The height of the pile in the nim game. This will be precisely the Nim value of the sole chococroc configuration if and only if the whole compound configuration (chococroc configuration + one pile Nim configuration) is a lost configuration for the first to move player. 
      player:
        regex: ^(0|1)$
        default: 0
        explain: If you to start as first player use 0, if you want to start as second player use 1.
      watch_value:
        regex: ^(no_watch|watch_winner|num_winning_moves|list_winning_moves|watch_grundy_val)$
        default: no_watch
        explain: With this flag you set which informations should be displayed about any configuration that you will face during your match.
      lang:
        regex: ^(hardcoded|hardcoded_ext|en|it)$
        default: it

  synopsis:
    evaluator: [python, services/synopsis/synopsis_server.py]
    args:
      service:
        regex: ^((\S)+)$
        default: synopsis
        explain: any string without space characters meaning one of the services of the problem {problem}
      lang:
        regex: ^(hardcoded|hardcoded_ext|en|it)$
        default: it
...
